{
  "name": "ecommerce-frontend-ui",
  "version": "1.0.0",
  "private": true,
  "scripts": {
    "build": "echo \"Building static frontend\" && mkdir -p build && cp public/index.html build/index.html"
  }
}
