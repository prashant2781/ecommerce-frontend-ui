{
  "name": "ecommerce-frontend-ui",
  "version": "1.0.0",
  "private": true,
  "dependencies": {
    "react": "^18.2.0",
    "react-dom": "^18.2.0"
  },
	"scripts": {
		"build": "npm install && npm run bundle && npm run copy-html",
		"bundle": "npx esbuild src/index.js --bundle --outfile=build/app.js",
		"copy-html": "cp public/index.html build/index.html"
	  }
}

